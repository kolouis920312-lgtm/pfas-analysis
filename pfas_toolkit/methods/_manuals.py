# -*- coding: utf-8 -*-
"""
_manuals.py — 每個分析方法的「使用說明書」內容（與方法邏輯分開放，保持方法檔乾淨）
================================================================================
每個方法兩層說明：
  beginner  讓初學者馬上看懂：這在做什麼、何時用、要什麼資料、參數怎麼設、結果怎麼看
  pro       補充專業知識：底層邏輯、數學/算法、關鍵選項差異、限制

內容用輕量 Markdown（## 標題、- 清單、**粗體**、`程式碼`、> 重點），前端會渲染成小視窗。
新增方法時，在這裡補一筆即可；methods/__init__.py 會自動掛到對應的 SPEC.manual。
"""

MANUALS = {

# ──────────────────────────────────────────────────────────── PCA
"pca": {
"beginner": """
## 一句話
把「很多欄位」壓縮成「少數幾個綜合指標」（主成分），讓你用一兩張圖就看出樣本之間的主要差異。

## 什麼時候用它
- 欄位太多、看不出重點時。
- 想畫散佈圖看樣本有沒有自然分群趨勢。
- 想先降維，再把結果丟給 K-means 分群或回歸。

## 需要什麼資料
- 一列＝一個樣本，多個**數值**特徵欄。
- 可以有一個 ID 欄（樣本名稱），它不會參與計算。

## 參數怎麼設（白話）
- **ID 欄**：樣本名稱欄，可留空。
- **排除欄/標籤欄**：不想納入分析的欄（例如分類標籤）。
- **保留主成分數**：先填 `0`（全部保留），跑完看「累積解釋變異圖」再決定要留幾個。

## 結果怎麼看
- **解釋變異圖（左）**：每個主成分能解釋多少資訊。**（右）累積線**碰到 90%／95% 時對應的主成分數，就是建議保留數（手肘）。
- **Biplot**：點是樣本、箭頭是原始特徵。箭頭**同方向＝正相關**，箭頭**越長＝對該主成分影響越大**。
- **pca_components.csv**：每個樣本的新座標（可直接餵給 K-means）。
- **pca_loadings.csv**：每個原始特徵對各主成分的貢獻。

> 小提醒：PCA 前會自動標準化；文字／類別欄會被自動忽略。
""",
"pro": """
## 底層邏輯
對標準化後的資料矩陣 X，求其共變異數矩陣的特徵分解（數值上等價於對 X 做 SVD），找出**變異數最大且彼此正交**的方向，即主成分。

## 算法步驟
1. `StandardScaler` 把每欄標準化成平均 0、標準差 1。
2. `PCA`（sklearn）求載荷矩陣 `components_` 與 `explained_variance_ratio_`。
3. 得分 `PC = X·W`，W 為共變異數矩陣的特徵向量（依特徵值由大到小）。

## 關鍵數學
- 第 i 個主成分解釋變異比例 = λ_i / Σλ（λ 為特徵值）。
- 載荷（loading）的**符號**代表特徵對該 PC 的方向，**大小**代表權重；Biplot 把樣本得分與特徵載荷疊在同一張圖。

## 限制與注意
- 只能擷取**線性**結構；對離群值敏感。
- 必須標準化（已內建），否則大尺度欄會主宰結果。
- **組成型（占比）資料**應先用本工具的 **CoDA** 方法做 CLR 轉換再 PCA，否則有「定和閉合」偽相關。
""",
},

# ──────────────────────────────────────────────────────────── K-means
"kmeans": {
"beginner": """
## 一句話
把樣本自動分成 k 組，讓**同組內彼此相似、不同組之間有差異**。

## 什麼時候用它
- 手上有數值座標（最常用 PCA 輸出的 PC 分數），想把樣本歸成幾群。
- 想用客觀指標決定「分幾群最好」。

## 需要什麼資料
- 一列＝一個樣本，多個數值欄（如 PC1、PC2…）。
- 可以有 ID 欄。

## 參數怎麼設（白話）
- **只用前幾維**：填 `0` 用全部；想只用前幾個主成分就填數字（例如 `5`）。
- **掃描的最大群數**：指標圖最多掃到幾群。
- **最終分群數**：先看指標圖，再決定要分成幾群。
- **亂數種子**：換個數字可檢查分群結果穩不穩定。

## 結果怎麼看
- **4 指標圖**：幫你挑 k——
  - Inertia：看「手肘」轉折點。
  - Silhouette：**越高越好**。
  - Calinski-Harabasz：**越高越好**。
  - Davies-Bouldin：**越低越好**。
- **散佈圖**：在前兩維上畫出各群與群中心（黑色 X）。
- **kmeans_labels.csv**：每個樣本屬於哪一群。
- **kmeans_cluster_members.csv**：每一群「有哪些樣本」（直接回答「哪幾筆是同一組」）。

> 小提醒：K-means 假設群是「大小相近的球狀」。形狀很不規則時，建議改用 HCA。
""",
"pro": """
## 底層邏輯
最小化**群內平方和**（inertia）：Σ_c Σ_{x∈c} ‖x − μ_c‖²。用 Lloyd 迭代：固定中心→指派每點到最近中心→重算中心，反覆到收斂。

## 算法細節
- `n_init=10`：用 10 組不同初始中心各跑一次取最佳，降低落入壞局部解的機率。
- `random_state=42`：可重現。

## 分群品質指標
- **Silhouette**：每點「與本群平均距離」對「與最近他群平均距離」的相對關係，∈[−1,1]。
- **Calinski-Harabasz**：組間離散 / 組內離散之比，越大越好。
- **Davies-Bouldin**：各群與最相似群的平均相似度，越小越好。

## 限制
- 需**事先決定 k**；對初始點與尺度敏感（建議先標準化或用 PC 分數）。
- 對非凸形狀、密度差異大的群表現差。
""",
},

# ──────────────────────────────────────────────────────────── HCA
"hca": {
"beginner": """
## 一句話
用**樹狀圖**把樣本「由近到遠」一步步合併成群；你可以決定要切成幾群，**或是在多大的距離以下就算同一組**。

## 兩種模式（最重要：先選「資料類型」）
這個 HCA 合併了通用與 PFAS 兩種用法，靠**「資料類型」**這個開關切換：
- **一般數值（標準化歐氏）**：任何數值表（血檢、感測器、各種特徵）。標準化後分群，最穩、最不挑資料。**預設就是這個**。
- **PFAS 組成（占比指紋）**：上傳 PFAS 化合物濃度／占比寬表時選它。會用組成資料的正確做法：
  先挑「大家都有測」的核心化合物盤、Bray–Curtis／CLR 雙距離、自動選群數、輸出各群「來源指紋」與
  cluster×論文／國家 交叉表；**沒測（空白）全程不會被當成 0**。

> 重點：PFAS 占比資料請**務必切到 PFAS 組成模式**；用一般模式會把「沒測」用中位數補值，扭曲組成。

## 什麼時候用它
- 還不確定要分幾群，想先看樣本的層級關係。
- 想知道「哪些樣本彼此最像」。
- （PFAS）想把跨研究的占比指紋分成幾種「來源型態」。

## 需要什麼資料
- 一列＝一個樣本，多個數值特徵欄（PFAS 模式即各化合物一欄）。
- 可以有 ID 欄（樹狀圖會顯示樣本名稱）。
- PFAS 模式：沒測請留**空白**，未檢出(BDL)才填 **0**；可另有 類別/相態、論文、國家欄做篩選與批次診斷。

## 參數怎麼設（白話）
- **連結方法**：決定「兩群之間的距離」怎麼算。新手用預設 `ward` 最穩（詳見「原理與算法」分頁）。
- **評估的最大群數**：指標圖要掃到幾群。
- **最終切割群數**：要切成幾群。
- **距離門檻（更細的控制）**：
  - 填 `0`：用上面的「群數」來切。
  - 填**大於 0**：改用「距離」切——樹狀圖會在這個高度橫切，**距離低於這個值的樣本就算同一組**（這就是「低於多少值以下算一組」）。群數會由資料自動決定。

## 結果怎麼看
- **樹狀圖**：高度＝距離，**越低就合併＝越像**；虛線是切割線。
- **群數評估圖**：Silhouette / Calinski-Harabasz 建議的群數。
- **各群輪廓圖**：每一群在各特徵上的平均（標準化後），看出每群的「個性」。
- **hca_results.csv**：每個樣本的群編號。
- **hca_cluster_members.csv**：每一群「有哪些樣本」（直接回答「哪幾筆是同一組」）。

> 小提醒：資料量大時，樹狀圖會自動摺疊（只顯示代表性分支）。
""",
"pro": """
## 底層邏輯
**凝聚式（agglomerative）階層分群**：一開始每個樣本各自成群，反覆把「最近的兩群」合併，直到合成一棵樹。合併過程記錄在 linkage 矩陣 Z（每列：合併了誰、距離多少、合併後幾個成員）。

## 連結方法（linkage）差異 — 常見問題
「兩群之間的距離」可以有不同定義，這會大幅改變分群形狀：
- **single（最近鄰）**：取兩群間**最近**一對點的距離。容易產生「鏈狀」串連，能抓不規則形狀，但對雜訊敏感、易產生長條群。
- **complete（最遠鄰）**：取兩群間**最遠**一對點的距離。傾向產生緊湊、大小相近的球狀群。
- **average（UPGMA）**：取兩群**所有點對的平均**距離，介於 single 與 complete 之間。
- **ward**：每次合併選「使**群內平方和增量最小**」的一對。傾向大小相近、變異小的群，搭配標準化＋歐氏距離，最常用也最穩。

## 切群與評估
- 切群用 `scipy ... fcluster`：
  - `criterion="maxclust"`：給**群數**。
  - `criterion="distance"`：給**距離門檻 t**，等於在樹狀圖高度 t 橫切。
- 評估：Silhouette、Calinski-Harabasz（掃描不同群數取較佳）。

## 限制
- 記憶體與時間約 O(n²)，超大資料較吃力。
- ward 預設搭歐氏距離；**標準化會明顯影響結果**（本工具已內建 `StandardScaler`）。

## PFAS 組成模式（進階）
切到「PFAS 組成」時改走組成資料方法學（沒測≠未檢出≠0）：
- **核心盤**：先依「覆蓋率門檻」剔除測得太少的化合物，避免把『沒測』誤當組成差異。
- **缺值兩法**：`complete`（核心盤完整觀測，含缺值列剔除→可同時跑 Bray–Curtis 與 CLR-Aitchison 雙距離對照）；
  `pairwise`（保留部分測量樣本，逐對只用『共同測項』算 Bray–Curtis，共同測項過少的對設最大距離保護）。
- **零替換**：BDL(0) 以乘法零替換（係數×各化合物最小正值）後做 CLR，避免 log(0)。
- **自動選群數**：silhouette 掃描，並在最高分的「精簡容差」內取**最小**群數，避免過度切分（純 argmax 常高估群數）。
- **群指紋**：以該群「全體樣本」的組成中心（complete＝CLR 中心轉回 %；pairwise＝各化合物實測平均並回報 coverage）。
- **批次診斷**：cluster×論文／國家 交叉表——若某群幾乎只來自同一篇論文或同一套測項，可能是批次/測項效應而非真實來源。
- 引擎沿用原 `pfas_hca`（已併入本方法，網站不再有第二個入口）。
""",
},

# ──────────────────────────────────────────────────────────── SOM
"som": {
"beginner": """
## 一句話
把高維的「PFAS 指紋」（多個物種濃度）攤平到一張 **2D 地圖**上，相似的樣本落在相近格子，藉此看出來源型態。

## 什麼時候用它
- 想視覺化多物種指紋的整體型態。
- 想比較不同**站別／季節**的型態是否相似。
- 想看哪些物種會「一起變高變低」。

## 需要什麼資料
- 一列＝一個樣本，多個物種濃度欄。
- 可選 ID／站別／季節欄。

## 參數怎麼設（白話）
- **納入的化合物**：想只看某幾隻 PFAS 的型態時勾選；不勾＝用全部。
- **只納入這些站別／季節**：想只比較某幾站／某幾季時勾選，會把其他樣本排除、對子集跑**一張** SOM。
- **SOM 列數 m × 行數 n**：地圖大小，越大越細緻但需要更多樣本。
- **訓練迭代數 / 初始學習率**：訓練強度，預設即可。
- **節點再分群數（regime）**：把地圖分成幾個來源型態區。
- **焦點站 A／B**：指定兩個站，額外輸出它們的型態重疊度。
> 每個參數「調大／調小會怎樣」的完整說明，請看 **⚙ 參數詳解** 分頁。

## 結果怎麼看（先建立心智圖像）
SOM 是一張 **m×n 的格子地圖**，每一格代表一種「典型指紋」，**相鄰格子＝指紋相似**；每個樣本被丟到最像它的那一格。

- **Component planes（就是你看到的「單隻 PFAS 圖」）**：這**不是**每隻 PFAS 各跑一次 SOM，而是**同一張地圖**、每張小圖只用**一種化合物**上色。亮＝那一格的典型指紋裡這隻濃度高、暗＝低。**看法：把幾張小圖疊著比**——在同一塊區域一起變亮的化合物＝常一起升降（可能同源）；亮區落在相反角落＝不同來源。
- **Regime 地圖**：把地圖切成幾個「來源型態區」並標上編號（顏色＝regime）；用來把「哪一塊是哪種來源型態」說清楚。
- **U-matrix**：相鄰格子的指紋差異；**亮＝差異大（不同群的邊界）**，暗成一片＝同一種型態的區域。
- **站／季落點圖**：每個樣本畫在它的格子上；**同色聚在同一區＝該站／季指紋一致**，散開＝型態多樣。
- **som_site_overlap.csv**：站對站的型態重疊度（0–1，越高越像）。

> 小提醒：建議先用 CoDA（CLR）轉換再丟進 SOM；若沒有，工具會自動做 z-score 標準化。
""",
"pro": """
## 底層邏輯
**自組織映射（Kohonen SOM）**：一層神經元排成格狀，每個節點有一個與輸入同維的權重向量（原型）。訓練時對每個樣本找最相似節點（BMU, Best Matching Unit），把 BMU 與其鄰域節點朝樣本方向拉近；鄰域半徑與學習率隨時間衰減，使地圖逐漸展開並保留拓樸關係。

## 關鍵公式
- BMU：`b = argmin_k ‖x − w_k‖`
- 更新：`w_k += lr(t)·h(d_{bk})·(x − w_k)`，其中鄰域 `h = exp(−d²/(2σ(t)²))`，lr 與 σ 隨 t 指數衰減。
- **regime**：對訓練後的節點權重再做 K-means，把地圖切成數個來源型態區。
- **站對站重疊度**：各站在 regime 上的落點比例後，逐 regime 取最小值再相加（overlap 係數）。

## 限制
- 有隨機性（本實作固定亂數種子以可重現）。
- 地圖大小、迭代數會影響結果；屬**型態探索**工具，不是嚴格的統計檢定。
""",
},

# ──────────────────────────────────────────────────────────── nonparam
"nonparam": {
"beginner": """
## 一句話
用「不假設常態分佈」的穩健統計，做三件事：**比較組間差異**、**檢查時間趨勢**、**算變數兩兩相關**。

> 下面括號裡的英文是統計方法的名字，**看不懂可以先略過**——工具會依你給的欄位自動挑對的方法。

## 什麼時候用它
PFAS 濃度通常右偏、又有很多 0（BDL），不適合 t 檢定／皮爾森相關。當你想問：
- 「不同季節／站別有差嗎？」
- 「濃度隨時間在上升嗎？」
- 「哪些物種會一起高？」

## 需要什麼資料
- 一列＝一個樣本，多個數值欄。
- 可選**分組欄**（如 season、site）做群差異；可選**時間欄**（如 date）做趨勢。

## 參數怎麼設（白話）
- **分組欄**：給了才做群差異（2 組用 Mann-Whitney，>2 組用 Kruskal-Wallis）。
- **時間欄**：給了才做趨勢（Mann-Kendall ＋ Sen 斜率）。
- **顯著水準 α**：判定「顯著」的門檻，預設 `0.05`；要更嚴格可改 `0.01`。
- 前三者皆可空，但分組欄／時間欄**至少要給一個**才有對應結果。

## 結果怎麼看
- **group_tests.csv**：每個變數的組間差異與顯著性（含 FDR 校正後的 `p_fdr`，`*` 代表顯著）。
- **trend_tests.csv**：每個變數的趨勢（上升／下降／無）與 Sen 斜率（每天變化量）。
- **相關熱圖 + spearman_rho/fdr.csv**：變數兩兩相關，圖上 `*` 代表「校正後 p < 0.05」才算顯著（FDR＝同時檢定很多項時，控制偽陽性的校正法）。
""",
"pro": """
## 用到的方法
- **群差異**：Mann-Whitney U（2 組）、Kruskal-Wallis H（>2 組）——皆為以「等級」運算的無母數檢定。
- **趨勢**：Mann-Kendall 檢定（單調趨勢的 S 統計量與 Z）＋ **Sen's slope**（所有點對斜率的中位數，抗離群）。
- **相關**：Spearman 等級相關 ρ。

## 多重比較校正
同時檢定很多變數會膨脹偽陽性，故全部套 **Benjamini-Hochberg FDR**，把 p 值轉成 q 值（`p_fdr`），控制偽發現率。

## 為什麼用無母數
以「等級」而非數值大小運算，對偏態、離群值、censored（BDL）資料**穩健**，不需常態假設。

## 限制
- Mann-Kendall 假設觀測間無強自相關；趨勢需可依時間排序。
- Spearman 偵測的是**單調**（非線性也可）的等級關係。
""",
},

# ──────────────────────────────────────────────────────────── xgboost_reg
"xgboost_reg": {
"beginner": """
## 一句話
用強力的樹模型（XGBoost）從多個特徵**預測一個連續數值**，並告訴你哪些特徵最重要。

## 什麼時候用它
- 想預測某個數值（例如某 PFAS 濃度）。
- 手上有多個可能影響它的因子，且關係可能是非線性。

## 需要什麼資料
- 一列＝一個樣本，多個數值特徵欄 **＋ 一個「目標欄 y」（必填）**。
- 可以有 ID 欄。

## 參數怎麼設（白話）
- **測試集比例**：留多少資料當「考試卷」評估（預設 0.2＝20%）。
- **樹的數量 / 樹深度 / 學習率**：模型複雜度；數量多、樹深、學習率高 → 學得細但容易過擬合。
- **樣本／特徵抽樣比例**：每棵樹隨機取一部分，降低過擬合。

## 結果怎麼看
- **評估**：測試集 R²（越接近 1 越好）、RMSE／MAE（越小越好）。
- **預測 vs 真實圖**：點越貼近虛線 `y=x` 代表預測越準。
- **特徵重要度圖**：哪些特徵貢獻最大。
- **SHAP 圖**（若有裝 shap 套件才會出現）：每個特徵把預測推高或拉低。

> 小提醒：樣本太少（只有幾十筆）容易過擬合；目標欄必須是數值。
""",
"pro": """
## 底層邏輯
**梯度提升樹（gradient boosting）**：逐棵樹去擬合「目前殘差的負梯度」，把多棵弱樹相加成強學習器。內建 L2 正則（`reg_lambda`）與列／行抽樣以抑制過擬合。

## 評估設計
- `train_test_split`（依 `test_size`）切出 hold-out 測試集。
- R² = 1 − SS_res/SS_tot；另報 RMSE、MAE。

## 可解釋性
- **gain 重要度**：該特徵在所有分裂帶來的損失下降總和。
- **SHAP**：基於賽局理論的可加性歸因，給每筆樣本每個特徵的貢獻（含方向），比單一全域重要度更細。

## 調參取捨
- `n_estimators` × `learning_rate` 互相取捨（小學習率配多樹）。
- `max_depth` 控交互作用複雜度；`subsample`／`colsample_bytree` 加隨機性、降方差。

## 限制
- in-sample 的重要度 **不等於因果**。
- 小樣本不穩、對分佈外的外推差。
""",
},

# ──────────────────────────────────────────────────────────── bdl
"bdl": {
"beginner": """
## 一句話
專門處理「未檢出」資料的工具箱：用**統計上正確**的方式填補低於偵測極限的值，而不是隨便填 0。

> **先懂兩個詞**：**BDL**＝低於偵測極限（儀器測不到的超低濃度，常被記成 0 或「ND」）；**MDL**＝方法偵測極限（能可靠測到的最低值）。

## 什麼時候用它
- 資料有很多 0 或「未檢出」。
- 想算平均、或要把資料餵給 PMF／下游分析，但不想因為亂填而造成偏差。

## 需要什麼資料
- 一列＝一個樣本，多個物種濃度欄。
- **BDL 請填 `0` 或留空白，不要寫 `ND`／`<MDL` 等文字**（會被當成錯誤）。

## 參數怎麼設（白話）
- **相對分析誤差**：建立「不確定度矩陣」用，典型 `0.1`（10%）。
- **固定 MDL**：填 `0` 自動用各物種最小正值；正式分析建議填實驗室真實 MDL。

## 結果怎麼看
- **detection_frequency**：各物種的偵測率。
- **subst_half / subst_sqrt2**：把未檢出當成「半個 MDL」或「MDL ÷ √2」的簡單替代法資料。
- **ros_imputed**：用統計回歸（ROS）推估未檢出值後的資料，比直接替代更不偏。
- **censored_summary**：各方法（censored＝含未檢出）估出的平均值比較。
- **uncertainty_matrix**：給 PMF 來源解析用的不確定度矩陣。
- **偵測熱圖**：哪些「樣本×物種」有檢出。

> 小提醒：此工具的 MDL 是用「各物種最小正值」推估；正式分析請改填實驗室真實 MDL。
""",
"pro": """
## 為什麼不能直接填 0
當 censored（未檢出）比例高時，用 0 或單一替代值會**系統性偏估**平均與變異。本工具提供多種統計法供比較。

## 方法
- **替代法**：BDL → MDL×0.5 或 MDL×(1/√2)。
- **ROS（Regression on Order Statistics）**：在對數空間，用未 censored 值對其常態分位回歸，外推出 censored 部分（需 ≥3 個檢出值）。
- **Tobit／截斷對數常態 MLE**：以最大概似估計真實母體平均（同時用到檢出值的密度與 censored 的累積機率）。

## 不確定度（供 PMF）
`U = √((err·x)² + (0.5·MDL)²)`；BDL 設 `5/6·MDL`（Polissar 類型加權）。

## 限制
- 估計品質隨偵測率下降；ROS 對重度 censored 不穩。
- MDL 的設定假設會影響所有結果。
""",
},

# ──────────────────────────────────────────────────────────── coda
"coda": {
"beginner": """
## 一句話
當資料是「**占比／組成**」（各物種的相對比例）時，先做對數比轉換，**移除「成分加起來固定」造成的假相關**，再去做 PCA／SOM。

> **名詞**：**CoDA**＝組成資料分析；**CLR**＝中心對數比，把「占比」換算成可以放心做一般統計的座標；**ILR**＝另一種沒有冗餘的版本。
> **為什麼會有假相關**：像切披薩，一塊變大、其他幾塊的「比例」必然變小——這種被迫的此消彼長不是真的關聯，對數比轉換就是要消掉它。

## 什麼時候用它
- 你關心的是「指紋型態（誰占多少）」而不是絕對濃度。
- 要餵 PCA／SOM 之前的前處理步驟。

## 需要什麼資料
- 一列＝一個樣本，多個成分（物種）欄。
- 可以有 ID 欄。

## 參數怎麼設（白話）
- **零替換係數**：把 0 替換成「係數 × 各物種最小正值」，預設 `0.65`。
- **同時輸出 ILR**：是否額外輸出 ILR 座標（預設開）。

## 結果怎麼看（此方法無圖，只有 CSV）
- **zero_replaced**：0 被穩健替換後的資料。
- **clr_transformed**：CLR 轉換結果，可直接餵 PCA／SOM。
- **ilr_transformed**：ILR 座標（可選）。

> 小提醒：零／BDL 比例 > 70% 時，CLR 對替換值很敏感，結果請謹慎詮釋。
""",
"pro": """
## 為什麼要轉換
組成資料受「**定和閉合**」限制（每列加總固定），落在單體形（simplex）上，直接套用歐氏空間的統計會產生**偽相關**（一個成分上升必使其他下降）。

## 轉換
- **CLR（中心對數比）**：`clr(x) = ln(x / g(x))`，g 為該列幾何平均。每列總和為 0、可逆，適合 PCA，但共變異數矩陣秩少 1（奇異）。
- **ILR（等距對數比）**：用正交基底（本實作用 Helmert 基底）把 CLR 映到 D−1 維歐氏座標，**無奇異性**。

## 零處理
**乘法零替換（multiplicative replacement）**：以 `δ = factor × MDL` 取代零並逐列維持閉合，避免破壞組成結構。

## 限制
- 對零替換值敏感；高零比例需更穩健的替換法。
- CLR 不適合需要滿秩共變異數的方法（此時改用 ILR）。
""",
},

# ──────────────────────────────────────────────────────────── ml_drivers
"ml_drivers": {
"beginner": """
## 一句話
用機器學習找出「**哪些環境／氣象因子在驅動 PFAS 濃度**」，用交叉驗證確認可靠，再用 SHAP／ALE 解釋影響方向。

## 什麼時候用它
- 有時間序列與多個協變數（邊界層高度 PBLH、風速、火點…）。
- 想知道「誰在影響濃度、怎麼影響」。

## 需要什麼資料
- 一列＝一個時間點，**一個目標欄 y（必填）**，多個數值協變數欄。
- 可選**日期欄**（有的話用時序交叉驗證）、站別欄。

## 參數怎麼設（白話）
- **目標欄**：要解釋／預測的濃度。
- **日期欄**：有就用「時序」交叉驗證（較符合真實預測情境）。
- **站別欄**：會被排除在特徵外。
- **交叉驗證折數**：預設 5。

## 結果怎麼看
- **cv_metrics**：RF／XGBoost 的交叉驗證 R²（平均±標準差）。
- **feature_importance**：哪些因子最關鍵（「置換重要度」＝把某因子打亂後，預測會變差多少）。
- **SHAP／ALE 圖**：每個因子把濃度往上或往下推、以及影響的形狀（SHAP 看每一筆的貢獻、ALE 看整體趨勢）。
- **hurdle_report**：當目標很多 0 時，拆成「有沒有檢出」與「檢出後高低」兩段來分析（hurdle＝跨欄，分兩關處理）。
""",
"pro": """
## 模型與驗證
- 模型：RandomForest（＋ XGBoost，若已安裝）。
- 交叉驗證：有日期欄用 `TimeSeriesSplit`（不洩漏未來），否則 `KFold`。

## 可解釋性
- **置換重要度（permutation importance）**：打亂某特徵後效能下降量，比純 gain 更公允、較不偏好高基數特徵。
- **SHAP**：可加性歸因，給每筆每特徵貢獻。
- **ALE（累積局部效應）**：對特徵相關較穩健，優於 PDP，呈現特徵與預測的關係形狀。

## Hurdle（零膨脹）
當零比例高（>15%）：用分類模型估「是否檢出」＋ 對正值部分做回歸，兩段組合。

## 限制
- 重要度 **不等於因果**；in-sample 的 Hurdle 指標偏樂觀（正式應用 CV）。
- 協變數共線會稀釋個別重要度（SHAP／ALE 可緩解詮釋）。
""",
},

# ──────────────────────────────────────────────────────────── trajectory
"trajectory": {
"beginner": """
## 一句話
用氣流的「**後向軌跡**」加上到站濃度，畫出污染最可能來自**哪個地理區域**的熱點圖。

## 什麼時候用它
- 想知道長程傳輸的「來源在哪裡」。
- 手上有 HYSPLIT／TrajStat 等產生的軌跡端點資料。

## 需要什麼資料（長格式）
一列＝一個**軌跡端點**，需要四欄：
- **軌跡編號**（同一條軌跡的所有端點共用一個編號）
- **緯度**、**經度**
- **到站濃度**（該軌跡對應採樣的受體濃度，沿整條端點重複填同一值）

## 參數怎麼設（白話）
- **四個欄位**：指定軌跡編號／緯度／經度／濃度對應到你資料的哪幾欄。
- **網格邊長（度）**：把地圖切成多大的格子。越小越細，但每格端點變少、雜訊變大。
- **高濃度百分位門檻**：濃度超過第幾百分位的軌跡算「高濃度」（PSCF 用）。

## 結果怎麼看
- **PSCF 圖**：高濃度的「條件機率」熱點＝潛在源區。
- **CWT 圖**：濃度權重的來源強度（可區分中度與強來源）。
- **trajectory_grid.csv**：各網格的 PSCF／CWT，可疊到地圖（QGIS／底圖）。

> **名詞**：PSCF＝潛在源貢獻函數、CWT＝濃度權重軌跡。
> **小提醒**：這種「軌跡端點」資料通常要先用 HYSPLIT／TrajStat 等後向軌跡模式產生；若你手上沒有，請先用其他方法。
""",
"pro": """
## 兩個指標
- **PSCF（潛在源貢獻函數）**：`PSCF_ij = m_ij / n_ij`，即「經過網格 ij 且屬於高濃度軌跡」的端點比例。只給條件機率，不分濃度高低。
- **CWT（濃度權重軌跡）**：`CWT_ij = Σ_l C_l·τ_ijl / Σ_l τ_ijl`，端點濃度以居留時間加權平均，補上「來源強度」資訊。

## 權重平滑
兩者皆乘上隨網格端點數 n_ij 遞增的經驗權重 W_ij（TrajStat 式），**抑制少端點網格造成的假熱點**。

## 高濃度軌跡判定
每條軌跡取代表濃度（端點濃度中位數），超過第 `threshold_pct` 百分位者視為高濃度。

## 限制
- 受軌跡模式誤差、端點密度、門檻選擇影響。
- PSCF 不分濃度大小、CWT 補強度；兩圖**搭配解讀**較可靠。
""",
},

# ──────────────────────────────────────────────────────────── pmf
"pmf": {
"beginner": """
## 一句話
把多個樣本的 PFAS 濃度矩陣，拆解成幾個「**來源指紋**」與「**各樣本的來源貢獻**」，量化每個來源占多少。

## 什麼時候用它
做**來源解析（source apportionment）**：「這些污染是哪幾類來源混出來的？各占幾成？」

## 需要什麼資料
- 一列＝一個樣本，**至少 3 個物種濃度欄（非負）**。
- 可以有 ID 欄。BDL 請填 `0` 或留空白。

## 參數怎麼設（白話）
- **來源因子數 p**：要拆成幾個來源。先試 3–6，看指紋能不能解讀。
- **相對測量誤差**：建不確定度用，典型 10–20%。
- **MDL**：偵測極限（不確定可留 0）。
- **最大迭代 / 亂數種子**：預設即可；換種子可檢查解穩不穩。

## 結果怎麼看
- **pmf_factor_profiles.csv**：各來源的物種指紋（每個因子一組組成）。
- **pmf_contributions.csv**：各樣本對各因子的貢獻。
- **pmf_source_share.csv**：各來源的平均貢獻佔比。
- **指紋圖 + 佔比圖**：比對指紋型態即可為來源命名（如長鏈 PFSA、前驅物）。

> 小提醒：樣本數最好是物種數的好幾倍，結果才穩。
""",
"pro": """
## 模型
`X ≈ G·F`，其中 X＝樣本×物種濃度、G＝樣本×因子貢獻、F＝因子×物種指紋，且 **G、F 皆非負**。
目標：`Q = Σ ((X − GF) / U)²`，以**測量不確定度 U 加權**的最小平方。

## 解法
權重 `W = 1/U²` 的乘法更新（Lee–Seung 加權版，等價 PMF 目標）；指紋 F 正規化到每列總和 = 1，尺度推回貢獻 G。

## 不確定度
`U = √((err·X)² + (0.5·MDL)²)`，再設資料尺度下限避免權重爆衝。

## 診斷
看 **Q / 自由度** 是否合理、**重建 R²**、以及指紋是否可解讀，來決定因子數 p。

## 與 PCA 的差別
- PMF：**非負＋不確定度加權**，因子可直接解讀為實體污染來源。
- PCA：允許負值、要求正交，主成分不可直接當「來源」。

## 限制
- 旋轉不確定（本實作未做 Fpeak）；解依初始值，建議多種子比較。
- 屬非唯一解，需專業判讀指紋命名。
""",
},

# ──────────────────────────────────────────────────────────── partitioning
"partitioning": {
"beginner": """
## 一句話
用化合物的 **log KOA** 算它在空氣中偏「**氣相**」還是「**粒相**」，這會決定它如何長程傳輸。

## 什麼時候用它
- 想判斷某 PFAS（如 FTOHs）是以**氣態飄遠**，還是隨**微粒沉降**。

## 需要什麼資料
- 一列＝一個化合物，需有 **log KOA 欄**。
- 可以有名稱欄。

## 參數怎麼設（白話）
- **log KOA 欄**：辛醇–空氣分配係數的 log10 值。
- **顆粒有機質分率 fom**：顆粒中有機質的質量分數，文獻常用 `0.1`。
- **總懸浮微粒 TSP（µg/m³）**：當地大氣顆粒物濃度，影響粒相比例。

## 結果怎麼看
- **gas_particle_partitioning.csv**：各化合物的 Kp、氣相比例、粒相比例、主導相。
- **堆疊長條圖**：每個化合物的氣／粒相比例（虛線為 50% 參考）。

> 小提醒：可調整 fom／TSP 做不同情境比較，馬上看到變化。
""",
"pro": """
## 模型（KOA 吸收模型，Pankow 1994 / Harner）
- `lg Kp = lg KOA + lg(fom) − 11.91`（Kp 單位 m³/µg）
- 粒相比例 `φ = Kp·TSP / (1 + Kp·TSP)`；氣相 `φ_gas = 1 − φ`

## 物理意義
- KOA 高（半揮發）→ 偏**粒相**，易隨微粒乾濕沉降。
- KOA 低（揮發）→ 偏**氣相**，隨氣團遠送，並沿途被 OH 氧化生成 PFCA。

## 限制
- 假設吸收平衡、固定溫度（約 25°C）、單一 fom。
- 僅含**吸收**（partitioning into organic matter），未含對礦物表面的**吸附**（KSA 機制）。
- TSP 與溫度變化會移動氣／粒分配。
""",
},

# ──────────────────────────────────────────────────────────── lrtp
"lrtp": {
"beginner": """
## 一句話
算每個化合物「能在空氣中存活多久、能飄多遠」，量化它的**長程傳輸潛力**。

## 什麼時候用它
- 想比較**前驅物與其氧化產物**誰飄得遠。
- 想找出誰才是真正的長程傳輸載體。

## 需要什麼資料
- 一列＝一個化合物，需有 **kOH 欄**（與 OH 自由基的反應速率常數）。
- 可選濕沉降 kwet、乾沉降 kdry 欄。
- 可以有名稱欄。

## 參數怎麼設（白話）
- **kOH 欄**：對應你資料的哪一欄。
- **平均 [OH]**：大氣 OH 自由基平均濃度，文獻常用 `1×10⁶`。
- **平均風速 u**：把壽命換算成距離用。
- **kwet／kdry 欄（可選）**：不填則只算光化學損失。

## 結果怎麼看
- **lrtp_ctd.csv**：各化合物的大氣壽命、半衰期、CTD（特徵遷移距離）、潛力等級。
- **CTD 長條圖**（對數軸，2000 km 為參考線）：越高代表飄越遠。

> 小提醒：CTD > 2000 km 屬高長程傳輸潛力。
""",
"pro": """
## 公式
- 光化學降解：`kdeg = kOH · [OH]`（s⁻¹）
- 總損失：`ktot = kdeg + kwet + kdry`
- 大氣壽命 `τ = 1/ktot`；半衰期 `t½ = ln2/ktot`
- 特徵遷移距離：`CTD = u·τ`（efold，衰減到 1/e）或 `u·(ln2/ktot)`（half，文獻常用的 50% 損失距離）

## 物理意義
以與 OH 反應為主要損失途徑；CTD 是濃度在順風方向衰減到 1/e（或一半）時的水平距離（van Pul / Bennett 概念）。

## 等級
CTD > 2000 km 為高、500–2000 km 為中、< 500 km 為低。

## 限制
- 單盒、定常風與定常 [OH] 的簡化假設。
- 未含再揮發／沉降後再起的回升過程。
- kOH 的不確定度對結果影響大。
""",
},

}


# 跨方法共用的「名詞詞彙表」（說明書小視窗的第三分頁；所有方法共用同一份）
GLOSSARY = """
## 統計與分群
- **標準化（z-score）**：把每欄換算成「平均 0、標準差 1」，讓不同單位的欄位可公平比較。
- **主成分／PC**：PCA 壓縮後的綜合指標；PC1 解釋最多差異，依序遞減。
- **載荷（loading）**：原始特徵對某個主成分的貢獻權重（含正負方向）。
- **群內平方和（inertia）**：每點到所屬群中心距離的平方和；越小代表群越緊密。
- **Silhouette／Calinski-Harabasz／Davies-Bouldin**：分群品質指標。前兩個**越高越好**、最後一個**越低越好**。
- **linkage（連結方法）**：HCA 決定「兩群距離」的方式：single 最近鄰、complete 最遠鄰、average 平均、ward 最小變異增量（最常用）。
- **樹狀圖（dendrogram）**：HCA 由近到遠合併樣本的樹；高度＝距離，越低合併越像。

## 顯著性與檢定
- **p 值 / 顯著**：結果「純屬巧合」的機率；p 越小越不像巧合。低於門檻（α）才算「顯著」。
- **顯著水準 α**：判定顯著的門檻，常用 0.05；要更嚴格可用 0.01。
- **FDR / p_fdr**：同時檢定很多項時，控制「偽陽性比例」的校正；`p_fdr` 是校正後的 p 值。
- **無母數**：不假設資料呈常態分佈的統計法，對偏態、離群、多 0 的資料較穩健。
- **Mann-Whitney U / Kruskal-Wallis**：比較 2 組 / 多組是否有差異的無母數檢定。
- **Mann-Kendall / Sen 斜率**：檢定時間趨勢是否單調（上升/下降），Sen 斜率是抗離群的變化率。
- **Spearman ρ**：用「等級」算的相關係數，偵測單調（非線性也可）關係，∈[−1, 1]。

## 組成資料（占比）
- **CoDA**：組成資料分析；處理「加總固定」的占比資料。
- **CLR / ILR**：對數比轉換。CLR＝中心對數比、ILR＝等距對數比，把占比換成可做一般統計的座標。
- **閉合（closure）**：占比加總被固定（如總和=1）造成的限制，會製造假相關。

## 未檢出 / 偵測極限
- **BDL**：低於偵測極限（儀器測不到的超低濃度，常記成 0 或「ND」）。
- **MDL**：方法偵測極限，能可靠測到的最低濃度。
- **censored**：含有未檢出（被「審查」）的資料。
- **ROS**：用統計回歸推估未檢出值的方法，比直接替代更不偏。
- **Tobit**：用最大概似估計含未檢出資料真實平均的模型。

## 機器學習
- **過擬合（overfitting）**：模型把訓練資料背得太死，換新資料就失準。
- **交叉驗證（cross-validation）**：把資料輪流切成訓練/測試多折，較公允地評估模型。
- **置換重要度**：把某特徵的值打亂後，模型變差多少，用來衡量它有多重要。
- **gain**：樹模型中，某特徵在所有分裂帶來的損失下降總和。
- **SHAP**：基於賽局理論，算每一筆、每個特徵把預測推高或拉低多少。
- **ALE（累積局部效應）**：呈現某特徵與預測的關係形狀，對特徵相關較穩健。
- **Hurdle**：目標很多 0 時，先判斷「有沒有檢出」、再分析「檢出後高低」的兩段式模型。

## 大氣長程傳輸
- **後向軌跡**：從受體站往回推算氣團來的路徑（常用 HYSPLIT 產生）。
- **PSCF**：潛在源貢獻函數；高濃度軌跡經過某網格的條件機率（找來源熱點）。
- **CWT**：濃度權重軌跡；把端點濃度加權平均到網格，給出來源強度。
- **KOA**：辛醇–空氣分配係數；越高越偏粒相、越低越偏氣相。
- **kOH / OH 自由基**：化合物與大氣 OH 自由基的反應速率常數；決定光化學降解快慢。
- **CTD（特徵遷移距離）**：化合物在空氣中能飄多遠的量化指標，越大代表長程傳輸潛力越高。
- **fom / TSP**：fom＝顆粒中有機質分率；TSP＝總懸浮微粒濃度，兩者影響氣/粒相分配。
"""


# ════════════════════════════════════════════════════════════════════
# 參數詳解（說明書第 2 分頁「⚙ 參數詳解」）：每個參數的意義 + 調整後如何改變結果
# ════════════════════════════════════════════════════════════════════
PARAM_MANUALS = {

"pca": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄，會設為索引、**不參與計算**。改它只影響輸出表的列名，不影響數值。
- **排除欄/標籤欄（可空）**：不想納入分析的欄（如分類標籤）。選了該欄就**不進 PCA**。
- **納入的特徵欄（不選＝全部）**：勾選要分析哪些數值欄。**勾子集** → 主成分與 biplot 只反映選到的特徵；**不勾** → 用全部數值欄。想聚焦某幾隻物種時很好用。
- **保留主成分數（0=全部）**：要保留幾個主成分。`0`＝全部；填 `k` → 只輸出前 k 個。**調大**保留更多資訊但維度高；**調小**更精簡。建議先填 0，看「累積解釋變異圖」碰到 90%/95% 再決定。
""",

"kmeans": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄，不參與計算。
- **納入的欄（不選＝全部）**：挑要分群的數值欄（例：只挑某幾個 PC）。勾子集 → 只用這些欄算距離與分群。
- **只用前幾維（0=全部）**：在（篩選後的）欄中**再**只取前 N 維。`0`＝全部。常用來「只用前幾個 PC」降噪。可與上一項並用（先挑欄、再取前 N）。
- **掃描的最大群數**：指標圖掃描 k=2…此值。**調大**能看更多 k 的指標但較慢。
- **最終分群數**：實際分成幾群。**調大**→群更多更細。先看 4 指標圖再決定。
- **亂數種子**：換數字可檢查分群**穩不穩**；若結果差很多代表分群不穩定（資料沒有清楚的群）。
""",

"hca": """
## 參數詳解（每個怎麼設、會怎麼改變結果）

### 最重要
- **資料類型（模式切換）**：`一般數值（標準化歐氏）`（預設）或 `PFAS 組成（占比指紋）`。PFAS 占比/濃度資料**務必選後者**，否則「沒測」會被中位數補值而扭曲組成。下面參數標【一般】或【PFAS】代表只在該模式生效。

### 共用
- **樣本 ID 欄（可空）**：樣本名稱欄，樹狀圖葉子會顯示它；留空自動編號。
- **特徵／化合物欄（不選＝自動全部數值欄）**：挑要分群的欄；勾子集→只用這些欄算距離。會自動忽略 ID/類別/論文/國家等非數值欄。
- **切割群數**：要切成幾群。【一般】距離門檻=0 時生效；【PFAS】關閉自動選群數時生效。調大→群更多更細。
- **群數評估／掃描上限**：群數評估圖、或自動選群數掃描 2～此值。
- **核心盤覆蓋率門檻**：只留『有測比例≥此值』的欄。`0`＝不過濾（一般模式預設）；**PFAS 模式未設時自動採 0.5**，建議 0.3~0.6。調高→盤更小更乾淨但保留樣本可能變少。

### 一般數值模式
- **排除欄／標籤欄（可空）**：要從特徵中排除的欄。
- **連結方法**：決定「兩群之間距離」怎麼算，**會大幅改變分群形狀**——`ward`（預設，最穩、傾向大小相近的緊湊群）、`complete`（緊湊球狀）、`average`（折衷）、`single`（鏈狀，能抓不規則但對雜訊敏感）。選 Bray–Curtis 距離時自動改 average。
- **距離類型（一般模式）**：`euclidean`（標準化歐氏，一般數值）、`braycurtis`（列封閉成占比）、`aitchison`（CLR 組成）。
- **距離門檻（0=用群數切）**：填 `>0` 改用「距離」切——樹狀圖在此高度橫切，**距離低於此值的樣本算同一組**，群數由資料自動決定。調高→群變少變大；調低→群變多變小。

### PFAS 組成模式
- **距離（組成 complete 模式）**：`both`＝同時跑 Bray–Curtis 與 CLR-Aitchison（建議對照）；pairwise 缺值模式固定 BC。
- **缺值處理模式**：`complete`（核心盤完整觀測，可雙距離、較嚴謹）或 `pairwise`（保留部分測量樣本，逐對只用共同測項算 BC）。
- **pairwise：最少共同測項數**：兩樣本共同測項少於此數→設最大距離，避免靠少數共同測項假裝相似（預設 5）。
- **用 silhouette 自動選群數**：開＝自動（取近似最佳中最精簡 k，避免過度切分）；關＝用上面「切割群數」。
- **群數精簡容差**：自動選群數時，在最高 silhouette 此容差內取最小群數；覺得群切太多可調高（預設 0.02，0＝純取最高分）。
- **零替換係數 ×偵測極限**：BDL(0) 乘法零替換值＝係數×各化合物最小正值（CLR 前處理，預設 0.65）。
- **類別／相態欄 + 要保留的值**：只跑某子集（如某相態，避免跨相態混分）。
- **論文欄／國家欄**：填了才輸出 cluster×論文／國家 批次效應交叉表。
""",

"som": """
## 參數詳解（每個怎麼設、會怎麼改變結果）

### 資料對應 / 篩選
- **ID 欄（可空）**：樣本名稱欄，不參與訓練。
- **站別欄 / 季節欄（可空）**：指定哪欄是站別／季節。**只用來**著色落點圖、做站對站重疊度，**不影響地圖訓練本身**。
- **納入的化合物（不選＝全部）**：勾選要餵進 SOM 的物種。**勾子集** → 地圖、component planes、regime 都只反映這些化合物（例：只想看長鏈 PFCA 的型態）；**不勾** → 全部數值欄。
- **只納入這些站別（不選＝全部）**：只想看某幾站時勾選，會把其他站的樣本**排除**，對子集跑「單一張」SOM；地圖、落點與重疊度都會跟著變。
- **只納入這些季節（不選＝全部）**：同理，依季節篩樣本。

### 地圖與訓練
- **SOM 列數 m × 行數 n**：地圖大小（節點數＝m×n）。**調大**→更細緻、能分更多型態，但需要更多樣本、訓練更久、每格樣本變少；**調小**→較粗略但穩。樣本不多時建議 5×5～7×7。
- **訓練迭代數**：訓練次數。**調大**→收斂更充分、結果更穩，但較慢；太小可能還沒收斂。
- **初始學習率**：每次更新幅度。**調大**→學得快但可能不穩；**調小**→平滑但需更多迭代。預設 0.5 即可。
- **節點再分群數（regime）**：把地圖切成幾個「來源型態區」（就是 **regime 地圖**的塊數）。**調大**→分更多細區；**調小**→更粗的大區。
- **焦點站 A／B（可空）**：指定兩站，額外輸出它們的**型態重疊度**與判讀（高/中/低）。填資料中沒有的站名會自動略過。
""",

"nonparam": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄，不參與檢定。
- **分組欄（做群差異，可空）**：**給了才做**群差異；2 組用 Mann-Whitney、>2 組用 Kruskal-Wallis。換不同分組欄＝換比較的維度（按季節比 vs 按站別比）。
- **時間欄（做趨勢，可空）**：**給了才做** Mann-Kendall 趨勢 ＋ Sen 斜率；需可依時間排序。
- **分析的變數欄（不選＝全部）**：挑要納入群差異／趨勢／相關的數值變數。勾子集 → 只分析這些變數，**相關熱圖也只含它們**（變數很多時可聚焦）。
- **顯著水準 α**：判定顯著的門檻（校正後 `p_fdr < α` 才標 `*`）。**調小**（如 0.01）→更嚴格、顯著項變少；**調大**→較寬鬆。
> 分組欄是「單選」：一次檢定對應一個分組維度；想換維度改選另一欄即可。
""",

"xgboost_reg": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄，不當特徵。
- **目標欄 y（必填）**：要預測的數值欄。
- **測試集比例**：留多少資料當「考試卷」（預設 0.2＝20%）。**調大**→評估更穩但訓練資料變少；**調小**→訓練多但評估較雜訊。
- **樹的數量**：弱學習器棵數。**調大**→學得更細、可能過擬合、較慢（建議配小學習率）。
- **樹深度**：每棵樹能抓的交互作用複雜度。**調大**→能抓更複雜關係但**易過擬合**。
- **學習率**：每棵樹的貢獻權重。**調小**→需更多樹但更穩、泛化更好；**調大**→快但可能學過頭。
- **樣本抽樣比例 / 特徵抽樣比例**：每棵樹隨機取一部分列／欄。**調小**→更多隨機性、**抑制過擬合**，但太小會欠擬合。
""",

"bdl": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄。
- **相對分析誤差（不確定度用）**：建「不確定度矩陣」用，典型 `0.1`（10%）。**調大**→不確定度變大，會改變後續 PMF 的加權。
- **固定 MDL（0=自動）**：`0`＝自動用各物種最小正值估 MDL；正式分析請填**實驗室真實 MDL**。**MDL 設定會連動影響**所有替代法、ROS 推估與不確定度結果。
""",

"coda": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄。
- **零替換係數 ×偵測極限**：把 0 替換成「係數 × 各物種最小正值」，預設 `0.65`。**調大**→替換值變大；**零/BDL 比例高時，CLR 對這個係數很敏感**，請多試幾個值看穩定性。
- **同時輸出 ILR**：是否額外輸出 ILR 座標（無奇異性，適合需要滿秩共變異數的方法）。開／關只影響有沒有多一個輸出檔。
""",

"ml_drivers": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄，不當特徵。
- **目標欄 y（必填）**：要解釋／預測的濃度。
- **日期欄（有則用時序 CV，可空）**：**有** → 用 `TimeSeriesSplit`（不洩漏未來、較符合真實預測情境）；**無** → 一般 `KFold`。
- **站別欄（排除於特徵，可空）**：指定後該欄**不當特徵**（避免把站別當預測因子）。
- **交叉驗證折數**：分幾折評估（預設 5）。**調大**→評估更細但較慢、每折資料變少。
""",

"trajectory": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **軌跡編號 / 緯度 / 經度 / 到站濃度欄**：把這四項對應到你資料的欄位（長格式，一列＝一個軌跡端點）。
- **網格邊長（度）**：把地圖切成多大的格子。**調小**→更細，但每格端點變少、雜訊大、易出**假熱點**；**調大**→較平滑但解析度低。
- **高濃度百分位門檻**：濃度超過第幾百分位的軌跡算「高濃度」（PSCF 用）。**調高**（如 90）→只看最高濃度軌跡、源區更聚焦但樣本變少；**調低**→納入更多軌跡。
""",

"pmf": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **ID 欄（可空）**：樣本名稱欄。
- **來源因子數 p**：要拆成幾個來源。**調大**→分更多來源，但可能拆過頭、指紋難命名；**調小**→來源會混在一起。先試 3–6，看指紋能否解讀。
- **相對測量誤差比例**：建不確定度用，典型 10–20%。影響加權與 `Q`。
- **方法偵測極限 MDL**：不確定可留 `0`；設定會影響不確定度下限。
- **最大迭代數**：**調大**→收斂更充分但較慢。
- **亂數種子**：PMF 解**非唯一**，換種子可檢查解穩不穩、是否得到一致的指紋。
""",

"partitioning": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **化合物名稱欄（可空）**：每列化合物的名稱。
- **log KOA 欄**：辛醇–空氣分配係數的 log10 值（核心輸入）。
- **顆粒有機質分率 fom**：顆粒中有機質的質量分數，文獻常用 `0.1`。**調大**→粒相比例上升。
- **總懸浮微粒 TSP（µg/m³）**：當地大氣顆粒物濃度。**調大**→粒相比例上升。可調這兩項做不同情境比較。
""",

"lrtp": """
## 參數詳解（每個怎麼設、會怎麼改變結果）
- **化合物名稱欄（可空）**：每列化合物的名稱。
- **kOH 欄**：與 OH 自由基的反應速率常數（核心輸入）。
- **平均 [OH]（molec/cm³）**：大氣 OH 濃度，常用 `1×10⁶`。**調大**→降解更快、大氣壽命變短、**CTD 變小**。
- **平均風速 u（m/s）**：把壽命換算成距離用。**調大**→CTD 線性變大。
- **濕沉降 kwet／乾沉降 kdry 欄（可空）**：不填則**只算光化學損失**；填了 → 總損失增加、壽命與 CTD 變小。
""",

}
