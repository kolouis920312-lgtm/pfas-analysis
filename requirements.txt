# PFAS 資料分析工具 — 網站版相依套件
# 安裝：pip install -r requirements.txt
Flask>=3.0,<4
gunicorn>=21            # 正式部署用的 WSGI 伺服器（Linux；Windows 本機開發用 python app.py 即可）

# 分析核心 pfas_toolkit 需要的科學運算套件
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10
xgboost>=2.0           # XGBoost 回歸 / 驅動因子 ML 兩個方法需要

# 選用：裝了會多畫一張 SHAP 摘要圖；沒裝也能跑（程式會自動略過）
# shap>=0.44

# 選用：裝了軌跡 PSCF/CWT 會多輸出「可縮放的互動式地圖（疊真實地形底圖）」
# 沒裝也能跑，只是少了互動地圖（仍有 matplotlib 靜態圖）。地形/地名圖磚免金鑰，需連網。
plotly>=5.24
